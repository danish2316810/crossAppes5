
_schema-version: "3.2"
ID: crosscrossapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: crosscrossapp-destination-content
  type: com.sap.application.content
  requires:
  - name: crosscrossapp-destination-service
    parameters:
      content-target: true
  - name: crosscrossapp-repo-host
    parameters:
      service-key:
        name: crosscrossapp-repo-host-key
  - name: crosscrossapp-uaa
    parameters:
      service-key:
        name: crosscrossapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: crosscrossapp_html_repo_host
          ServiceInstanceName: crosscrossapp-html5-srv
          ServiceKeyName: crosscrossapp-repo-host-key
          sap.cloud.service: crosscrossapp
        - Authentication: OAuth2UserTokenExchange
          Name: crosscrossapp_uaa
          ServiceInstanceName: crosscrossapp-xsuaa-srv
          ServiceKeyName: crosscrossapp-uaa-key
          sap.cloud.service: crosscrossapp
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: crosscrossapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: crosscrossapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - crosscrossapp.zip
      name: crosscrossapp
      target-path: resources/
- name: crosscrossapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: crosscrossapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: crosscrossapp-destination-service
    service-plan: lite
- name: crosscrossapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: crosscrossapp-xsuaa-srv
    service-plan: application
- name: crosscrossapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: crosscrossapp-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
